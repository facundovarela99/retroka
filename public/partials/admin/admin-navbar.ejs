<nav class="app-header navbar navbar-expand bg-body border-bottom" aria-label="Navegación administrativa superior">
    <div class="container-fluid">
        <ul class="navbar-nav align-items-center">
            <li class="nav-item">
                <button
                    class="nav-link btn btn-link"
                    type="button"
                    data-lte-toggle="sidebar"
                    aria-label="Alternar menú lateral"
                >
                    <i class="bi bi-list" aria-hidden="true"></i>
                </button>
            </li>
            <li class="nav-item d-none d-sm-block">
                <a class="nav-link" href="<%= url('/productos') %>">
                    <i class="bi bi-shop me-1" aria-hidden="true"></i>
                    Ver tienda
                </a>
            </li>
        </ul>

        <ul class="navbar-nav ms-auto align-items-center">
            <li class="nav-item dropdown">
                <button
                    class="nav-link dropdown-toggle btn btn-link d-flex align-items-center gap-2"
                    type="button"
                    data-bs-toggle="dropdown"
                    aria-expanded="false"
                    aria-label="Abrir menú de usuario"
                >
                    <i class="bi bi-person-circle fs-5" aria-hidden="true"></i>
                    <span class="d-none d-md-inline"><%= user.email %></span>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li>
                        <span class="dropdown-item-text text-body-secondary small">Administrador</span>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                        <form method="post" action="<%= url('/logout') %>">
                            <input type="hidden" name="csrf_token" value="<%= csrf_token %>">
                            <button class="dropdown-item" type="submit">
                                <i class="bi bi-box-arrow-right me-2" aria-hidden="true"></i>
                                Cerrar sesión
                            </button>
                        </form>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>
